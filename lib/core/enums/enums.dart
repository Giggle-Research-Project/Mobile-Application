enum DyscalculiaType { PROCEDURAL, SEMANTIC, VERBAL }

enum Difficulty { EASY, MEDIUM, HARD }

enum Lesson {
  ADDITION,
  SUBTRACTION,
  MULTIPLICATION,
  DIVISION,
  NUMBERS,
  COMPARISON,
  ODDEVEN,
  DAYS,
  FRACTION,
  MONTHS,
  MATHWORDS,
  TIME
}

enum TestType { skillAssessment, parentQuestionnaire }

enum TestScreenType { skillAssessment, parentQuestionnaire }
